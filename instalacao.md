Se o código não rodar de primeira, rode os seguintes comandos:

```bash
# Ativar o ambiente virtual
.\.venv\Scripts\Activate.ps1
# Instalar dependências
pip install -r requeriments.txt
# Rodar o código
python newton-rapson.py
```